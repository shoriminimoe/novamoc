# Optional API base URL override. Leave empty to use Vite's dev proxy,
# which forwards /auth, /schema, /events, /problems, /openapi to
# NOVAMOC_API_URL (default http://127.0.0.1:8000). Requests carry the
# session cookie via ``credentials: 'include'`` (ADR-020).
VITE_API_BASE_URL=
